apiVersion: apps/v1
kind: Deployment
metadata:
  name: tissea-back
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tissea-back
  template:
    metadata:
      labels:
        app: tissea-back
    spec:
      containers:
        - name: tissea-back
          image: harbor.kakor.ovh/ipi/ipi-grp-2-back:latest
          ports:
            - containerPort: 3000